import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  turbopack: {
    root: "/Users/arfannazar/Claude Code/voice-of-gen-2026",
  },
  allowedDevOrigins: ["127.0.0.1", "localhost"],
};

export default nextConfig;
